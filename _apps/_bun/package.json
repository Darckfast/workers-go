{
  "name": "bun",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run build:go && bun run --watch index.ts",
    "test": "bun run build:go && bun test",
    "build:go": "GOOS=js GOARCH=wasm go build -o ./bin/app.wasm ./main.go"
  },
  "devDependencies": {
    "@types/bun": "1.3.14"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
