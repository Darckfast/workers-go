on:
  push:
    branches:
      - main

permissions:
  issues: write
  contents: write
  pull-requests: write

name: release-please

jobs:
  release-please:
    runs-on: codeberg-tiny-lazy
    steps:
      - uses: https://github.com/googleapis/release-please-action@v5
        with:
          release-type: go
